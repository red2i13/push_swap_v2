#include "../includes/push_swap.h"

int find_num(int num, int *arr, int size)
{
    int i;
    int min;
    int max;
    int mid;
    
    min = 0;
    max = size - 1;
    i = 0;
    while (min <= max)
    {
        mid = (max - mid) / 2;

    }
    
}
int main()
{
    
}